[project]
name = "unmute-data-pipeline"
version = "0.1.0"
description = "Scrapes the NTU SgSL Sign Bank and builds the vocab.json/signs_metadata.json consumed by the Unmute backend"
requires-python = ">=3.11,<3.13"
dependencies = [
    "requests>=2.33.1",
    "beautifulsoup4>=4.14.3",
    "tqdm>=4.67.3",
]

[tool.uv]
package = false
